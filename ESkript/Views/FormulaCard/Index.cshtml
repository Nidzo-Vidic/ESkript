@model IEnumerable<ESkript.Models.FormulaCard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Comment.IdComment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScriptContent.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Formula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comment.IdComment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScriptContent.Titel)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Formula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFormulaCard }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFormulaCard }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFormulaCard })
        </td>
    </tr>
}

</table>
