@model IEnumerable<ESkript.Models.SourceCodeCard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScriptContent.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourcceCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScriptContent.Titel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourcceCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSourceCodeCard }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSourceCodeCard }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSourceCodeCard })
        </td>
    </tr>
}

</table>
